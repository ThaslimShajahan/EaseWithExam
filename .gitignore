node_modules/
dist/
dev-dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.claude/
.qodo/

# Build/deploy artifacts and large binaries — not source.
*.zip
IMG_4429.MOV
data.txt
supabase/.temp/

# Local source PDFs staged for one-time ingestion into knowledge_base — not
# app source, ~1.6GB, never meant to be committed.
easy with exam/
scripts/.ncert-ingest-checkpoint.json

# Raw screenshots captured by scripts/landing-shots.mjs. Only the cropped
# results in public/landing/ are committed; these are the large intermediates.
.landing-shots/

# Credentials — never commit.
*firebase-adminsdk*.json
secrets/

# Transient artifact of scripts/make-scanned-fixture.mjs (the fixture PDF itself IS committed).
test-fixtures/_shell.html
public/_pilot/

# Bulk loader run state (resumable checkpoints, not source)
.corpus-load-checkpoint.json
.pyq-load-checkpoint.json

# Answer-quality benchmark run state (regenerable, not source)
.benchmark/

# Scratch HTML that scripts/og-image.mjs renders through Playwright. Only the
# result, public/og-image.png, is committed.
.og-build/

# Ad-hoc SEO verification harnesses, run from the repo root so they can resolve
# playwright. Not part of the suite — the committed assertions live in
# src/lib/__tests__/seoRoutes.test.js.
.verify-seo.mjs
.verify-prod.mjs
.count-users.mjs
.shot404.mjs
.shot-*.png

# Transient output of scripts/audit-admin-rpc-grants.mjs introspection runs.
.admin-rpcs.json
.admin-rpc-audit.json
.diag-token.mjs
.verify-gate.mjs

# Generated P0.5 batch output before it is promoted into supabase/migrations/
.p05-batch*.sql
.p05-inventory.json
.diag-*.mjs

# One-off scratch from the currentUser scope investigation.
.scan-tmp/
.scan-scope.mjs
